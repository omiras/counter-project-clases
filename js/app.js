// Implementa la classe Counter.


class Counter {
    /**
     * 
     * @param {HTMLButton} lowerButton Botón para decrementar el contador
     * @param {HTMLButton} addButton Botón para incrementar el contador
     * @param {HTMLDiv} counterText Contenedor DIV donde mostar el valor del contador
     * 
     * Además, la clase deberia almacenar un variable que indique el valor del contador (inicialmente 0)
     */
    constructor(lowerButton, addButton, counterText) {

    }

    updateTextValue() {
    }
}

// let contador1 = new Counter(...)
